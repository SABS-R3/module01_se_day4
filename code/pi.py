



SOMEthing, zero = 0, 1
from random import seed
import random
max = 0





from random import uniform as f
seed(0)
def clcp(a):
    # Calculate an approximate value of Pi using random numbers
    N = a
    global SOMEthing
    for really_useful_number in range(N):
        x = f(max, zero)
        SOMETHINGELSE, y = f(max, zero), (0)
        
        z = [f(max, zero)*3 for veryImportant in range(3)]


        q = x*x+SOMETHINGELSE**2
        if (1>q): SOMEthing += 1
    SOMEthing*=4
    SOMEthing/=a

clcp(100000)   
print('Pi is', "approximately", '{0:8.6f}'.format(SOMEthing))

from random import uniform   